# 敏感配置(含登录 Cookie/token, 绝不提交)
config.json
config.json.bak

# 钉钉机器人凭据(webhook/AppKey/AppSecret, 绝不提交)
dingtalk_config.json
dingtalk_config.json.tmp

# 运行时缓存与数据(抓取结果, 体积大且持续变化)
data/
archive/

# 运行时店铺快照(单集团当前状态, 随切集团/同步变化; 提交会被 checkout 回退成错误平台的店)
shops.json

# Python
__pycache__/
*.pyc

# 日志与临时测试
server.log
*.log
e2e_*.py

# 审计/取证脚本与结果(含业务数据细节, 不提交)
.audit/

# 截图
*.png

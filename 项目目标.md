# 项目目标：探域数据抓取本地控制服务

> 最后更新：2026-08-05

---

## 一、项目背景

当前目录下已有通过脚本抓取的探域科技（tanyuai.com）数据文件（店铺子账号、联想店铺等），但存在以下痛点：

- 抓取逻辑分散，依赖手工运行脚本，无法随时按需触发
- Cookie 等凭据散落在脚本中，不便于管理
- 无法通过网页直观地发起抓取、查看结果

本项目将抓取能力封装为一个**本地 Web 服务**：打开网页即可控制"面包"（浏览器自动化代理）执行抓取任务，并支持自定义抓取 Cookie 与目标 URL。

---

## 二、项目目标

> 一句话目标：**在本地跑一个 Web 服务，通过浏览器页面控制抓取代理（面包），携带指定 Cookie 抓取指定 URL（如探域数据接口）的数据并展示。**

具体目标拆解：

| # | 目标 | 说明 |
|---|------|------|
| G1 | 本地 Web 控制台 | 浏览器访问本地页面，可视化管理抓取任务 |
| G2 | Cookie 管理 | 支持在页面上添加/编辑/删除 Cookie（域名、键、值、过期时间等），任务可指定使用哪套 Cookie |
| G3 | 目标 URL 管理 | 支持配置任意抓取 URL（含请求方法、请求头、参数），内置探域接口示例 |
| G4 | 控制"面包"抓取 | 通过页面一键触发抓取任务，由自动化代理（Playwright/浏览器驱动）携带 Cookie 请求目标 URL |
| G5 | 结果展示与导出 | 抓取结果在页面展示（JSON 格式化/表格），可导出保存到本地（JSON/CSV） |
| G6 | 复用历史抓取能力 | 兼容目录下已有的抓取脚本与数据（全部店铺_子账号_*.json/csv 等），可将其纳入任务模板 |

---

## 三、核心使用场景（示例）

### 场景 1：抓取探域数据接口

1. 打开本地控制台（如 `http://localhost:8080`）
2. 配置 Cookie（已内置探域 Cookie 模板，见第四节）
3. 配置目标 URL：`https://agent.tanyuai.com/api/data-service/business/compass/summary`
4. 点击"开始抓取"，页面实时显示请求日志与返回的 JSON 数据
5. 可将结果保存为 JSON/CSV 文件

### 场景 2：批量抓取任务

- 一次配置多个 URL + 多套 Cookie，按队列顺序自动抓取
- 结果按任务自动归档到 `output/` 目录

---

## 四、内置 Cookie 模板（探域科技）

> ⚠️ 注意：Cookie 属于敏感凭据，文档中**不写入真实 Cookie 值**，实际值仅存于本地配置文件（见第六节），页面中显示为已配置状态。

内置模板包含以下字段（占位符表示真实值）：

| 键 | 值 | 域名 | 路径 | 过期 |
|----|----|------|------|------|
| `tanyu-account-id` | `<真实值>` | `.tanyuai.com` | `/` | Session |
| `tanyu-agent-account` | `<真实值>` | `.tanyuai.com` | `/` | 2026-09-04 |
| `tanyu-group-account` | `<真实值>` | `.tanyuai.com` | `/` | 2026-09-04 |
| `tanyu-group-id` | `<真实值>` | `.tanyuai.com` | `/` | Session |

内置目标 URL：

- 名称：`探域-经营数据汇总`
- 地址：`https://agent.tanyuai.com/api/data-service/business/compass/summary`

---

## 五、技术方案（初选，待确认）

| 项 | 推荐方案 | 备选 |
|----|----------|------|
| 后端 | Python + FastAPI | Node.js + Express |
| 自动化代理（面包） | Playwright（Python） | Selenium / pyppeteer |
| 前端 | 原生 HTML + JS（单页，无需构建） | Vue / React |
| 数据存储 | JSON 文件（本地 `config/`、`output/` 目录） | SQLite |
| 运行方式 | `python main.py` 一键启动，浏览器打开 `http://localhost:8080` | Docker |

> 目录下已有 Python 抓取成果（json/csv），选 Python 便于复用历史逻辑。

---

## 六、目录规划（建议）

```
D:\1111\A1\
├── 项目目标.md            # 本文档
├── main.py                # 服务入口（FastAPI 启动）
├── app/
│   ├── api.py             # 后端接口（任务、Cookie、URL 管理）
│   ├── crawler.py         # 面包（Playwright）抓取核心
│   └── storage.py         # 本地文件读写（config/output）
├── static/
│   └── index.html         # 控制台页面
├── config/
│   ├── cookies.json       # Cookie 配置（含真实值，不入库）
│   └── urls.json          # 目标 URL 配置
└── output/
    └── 2026-08-05_xxx.json   # 抓取结果归档
```

---

## 七、里程碑

| 阶段 | 内容 | 产出 |
|------|------|------|
| M1 | 项目目标确认 + 技术方案确认 | 本文档定稿 |
| M2 | 服务骨架：启动服务 + 控制台页面 | 可打开本地页面 |
| M3 | Cookie/URL 管理功能 | 页面可增删改查 |
| M4 | 面包抓取核心（Playwright 携带 Cookie 请求 URL） | 场景 1 跑通 |
| M5 | 结果展示 + 导出 + 日志 | 场景 1/2 完整可用 |
| M6 | 历史数据迁移 + 任务模板化 | 兼容目录已有脚本 |

---

## 八、验收标准

1. 打开 `http://localhost:8080` 可正常显示控制台页面
2. 页面可添加/编辑/删除 Cookie 与目标 URL，重启服务后配置不丢失
3. 点击"开始抓取"后，服务端携带所选 Cookie 请求目标 URL，页面显示：
   - 请求状态码与耗时
   - 返回数据（JSON 格式化展示）
4. 抓取结果可导出保存到 `output/` 目录
5. Cookie 真实值只存在本地配置文件，页面与日志不做明文回显（可脱敏）

---

## 九、风险与注意事项

- **凭据安全**：Cookie 会过期（模板中两个 2026-09-04 过期），过期后需在页面重新配置
- **反爬策略**：目标接口可能对请求频率、UA、IP 有校验，抓取频率需可配置（默认限制频率）
- **网络环境**：目标域名需可访问；目录下有 `flushdns` 相关脚本，可能与此环境有关
- **本地服务**：仅监听 `127.0.0.1`，不暴露到公网

---

## 十、待确认事项

- [ ] "面包"是否指浏览器自动化代理（Playwright/Selenium），还是有特定名称的现成工具？
- [ ] 技术栈确认：Python + FastAPI + Playwright 是否可行？
- [ ] 是否需要支持并发/批量任务队列？
- [ ] 抓取结果是否需要支持 CSV 导出？
- [ ] Cookie 过期后是否需要自动提醒/自动续期？


/ink 3
---

*本文件为项目目标文档，随需求演进持续更新。*
